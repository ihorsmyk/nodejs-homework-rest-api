const { contactAddSchema } = require("./contacts");
const { userRegisterSchema, userLoginSchema } = require("./users");

module.exports = {
  contactAddSchema,
  userRegisterSchema,
  userLoginSchema,
};
