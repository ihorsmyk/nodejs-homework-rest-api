const { contactAddSchema } = require("./contacts");
const { userRegisterSchema, userloginSchema, emailSchema } = require("./users");

module.exports = {
  contactAddSchema,
  userRegisterSchema,
  userloginSchema,
  emailSchema,
};
